<VirtualHost *:80>
    DocumentRoot /var/www/html/public
    
    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted
        FallbackResource /index.php
    </Directory>
    
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
```

**8.** Sauvegardez (Ctrl+S ou Cmd+S)

---

##  Votre structure doit ressembler à ça :
```
mediatekformation/
├── bin/
├── config/
├── docker/                    ← NOUVEAU DOSSIER
│   └── apache.conf           ← NOUVEAU FICHIER
├── migrations/
├── public/
├── src/
├── templates/
├── var/
├── .env
├── .gitignore
├── composer.json
├── composer.lock
├── Dockerfile                ← NOUVEAU FICHIER
└── symfony.lock