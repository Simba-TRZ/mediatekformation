{{ form_start(form) }}

<div>
    <div class="row">
        <div class="col">
            {{ form_row(form.title) }}
        </div>
        <div class="col">
            {{ form_row(form.description) }}
        </div>
        <div class="col">
            {{ form_row(form.publishedAt) }}
        </div>
    </div>

    <div class="row" style="margin-top: 1rem">
        <div class="col">
            {{ form_row(form.videoId) }}
        </div>
        <div class="col">
            {{ form_row(form.categories) }}
        </div>
        <div class="col">
            {{ form_row(form.playlist) }}
        </div>
    </div>
</div>

<div>
    <div class="align-content-center justify-content-center" style="display: flex; margin-top: 1rem;">
        {{ form_row(form.submit) }}
        <div class="mb-3" style="padding-left: 5rem">
            <a href="{{ path('backoffice_formations_index') }}" class="btn btn-secondary">
                Annuler
            </a>
        </div>
    </div>
</div>

{{ form_end(form) }}
